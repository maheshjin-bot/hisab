# Supabase — HISAB's own dedicated project (never point this at another app's project).
# Get these from the project's API settings once it's provisioned.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
