// TODO(hisab): PLACEHOLDER. Regenerate this file for real once the
// hisab-production Supabase project exists and migrations 0001-0007 are
// applied, via the Supabase MCP `generate_typescript_types` tool (see Task #9
// in the build plan). Until then this is intentionally untyped so the rest of
// the app can compile against a stable `Database` import path.
// eslint-disable-next-line @typescript-eslint/no-explicit-any -- placeholder, see above
export type Database = any;
